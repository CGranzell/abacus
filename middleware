// import { NextRequest, NextResponse } from 'next/server';


// export function middleware(req , NextRequest) {

  // Check if user has cookie

  // if not, assing userID



// Skapa cookie
  // const response = NextResponse.next({
  //   request: {
  //    headers: req.headers
  //   },

  // });

  // response.headers.forEach((header) => console.log('header', header));

  // response.headers.set('x-custom-auth-header', 'isAuthed')

  // response.cookies.set('isAuthed44', 'true');
  // console.log('response', response.cookies);

  // console.log('response headers', response.headers);

  // Radera cookie
  // req.cookies.delete('auth');

  // Hämta en specifik cookie
  // console.log('isAuth?', req.cookies.has('auth'));

  // Hämta alla cookies
  // const allCookies = req.cookies;
  // console.log('allCookies', allCookies);

  // Hämta en cookie
  // const cookie =req.cookies.get('auth');
  // console.log('cookie', cookie);

  // return NextResponse.next();
//   return response;
// }

// export const config = {
//   matcher: '/'
// }